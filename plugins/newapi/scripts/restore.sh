#!/bin/bash
# newapi restore — Restore from backup
set -eo pipefail

BACKUP_DIR="${NEWAPI_BACKUP_DIR:-/opt/newapi/backups}"
echo "[newapi] Restoring from ${BACKUP_DIR}..."
echo "[newapi] Restore stub complete."
