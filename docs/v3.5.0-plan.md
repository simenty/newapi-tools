# V3.5.0 规划方案

> **当前版本**：v3.4.1（2026-06-01 发布）  
> **规划时间**：2026-06-01  
> **状态**：草案

---

## 一、现状评估

### ✅ 已就绪的

- 全部 16 包测试通过
- 覆盖率门禁 ≥30% 达标
- CI/CD 三工作流稳定
- 5 平台自动发布
- 文档站完整（含 instance/audit/错误码）
- Config Validate / audit 轮转 / gendocs hook
- v3.4.1 已发布

### ⚠️ 仍存在的

- `internal/cli` 覆盖率仅 17.4%（低）
- `internal/docker` HTTP API 客户端已编写但未集成（候选功能）
- Docs 页面未在 Linux 实机预览确认

---

## 二、候选方向

按优先级从高到低排列：

### A. 工程基建加固

| 方向 | 内容 | 工作量 |
|------|------|--------|
| A1 | 补齐 cli 测试至 ≥30% | ⭐⭐⭐ |
| A2 | 补齐 osutil 测试至 ≥30%（当前 28.4%） | ⭐ |
| A3 | CI 中增加 `go mod tidy` 检查，防止 go.sum 漂移 | ⭐ |
| A4 | 新增 `.github/dependabot.yml` 自动检测依赖更新 | ⭐ |

### B. Docker API 客户端集成

TRAE 已实现 `internal/docker/http_client.go`（基于 net/http）和 `sdk_client.go`（接口定义）。  
v3.5.0 可正式集成：

| 步骤 | 内容 | 说明 |
|------|------|------|
| B1 | 确认 http_client 覆盖足够场景 | 与现有 exec.Command 并行可用 |
| B2 | 新增 `docker --mode` flag | 切换 CLI / HTTP 模式 |
| B3 | 逐步迁移 ContainerList / ImagePull | 从 exec 迁移到 HTTP API |
| B4 | 测试双模式一致 | 确保两种路径输出相同 |

**优点**：减少 Shell 注入面、更精细的错误信息、不依赖 docker CLI 版本  
**风险**：需要额外授权配置（Docker socket 权限）

### C. 用户体验（CLI）

| 方向 | 内容 | 工作量 |
|------|------|--------|
| C1 | Shell 自动补全（bash/zsh/fish） | ⭐ |
| C2 | `doctor` 增加网络连通性检查项 | ⭐ |
| C3 | `doctor` 增加 Go 版本检查（远程最新版 vs 当前版） | ⭐ |
| C4 | `install --interactive` 增加代理配置步骤 | ⭐ |

Cobra 内置 shell completion 支持（`cmd/handoff.go`）+ `__complete` 命令。  
实现方式：在 rootCmd 上调用 `completionCommand`，生成补全脚本。

### D. 安全加固

| 方向 | 内容 | 工作量 |
|------|------|--------|
| D1 | GitHub Release 产物 GPG 签名 | ⭐ |
| D2 | `update --self` 启用 RequireSHA256（v3.3.5 已实现） | ⭐ |
| D3 | docs/security.md 安全最佳实践文档 | ⭐ |

---

## 三、推荐路线

```
v3.4.1 ← 当前
  │
  ├── A1+A2 (测试补齐) → 基础质量
  ├── C1 (自动补全) → 高频体验
  ├── B1+B2 (Docker API 评估) → 技术升级
  └── C2+C3 (doctor 增强) → 诊断能力
  │
v3.5.0 ← 目标
```

### 建议优先级排序

```
P0: A2 (osutil 测试) → 最快，28.4%→30% 门槛
P0: A1 (cli 测试) → 17.4%→30%，影响覆盖率
P0: A3 (go mod tidy CI) → 预防性
P1: C1 (自动补全) → 高频功能，用户感知强
P1: C2 (doctor 网络检查) → 诊断增强
P2: B1+B2 (Docker API 评估) → 不急于替换
P2: D1+D2 (签名+校验) → 安全加分
```

---

## 四、已确认决策

以下决策由项目负责人确认：

| # | 问题 | 决定 |
|---|------|------|
| 1 | Docker API 客户端是否正式启用？ | **B. v3.5.0 正式集成** |
| 2 | 是否需要新增 CLI 命令？ | **B. 新增 config validate 子命令** |
| 3 | Shell 补全用 Cobra 内置，还是自定义？ | **A. 内置 generate completion** |
| 4 | 发布时间？ | A. 等 handoff 任务清仓后 |

---

## 五、TRAE 执行清单

按优先级排列，TRAE 在 handoff 分支上逐项完成：

### Task V1: Shell 自动补全（优先做）

**文件**：`cmd/newapi/main.go` + 新增 `cmd/completion/`  
**方式**：Cobra 内置 `completion` 命令

```go
// 在 rootCmd 上注册 completion 子命令
var completionCmd = &cobra.Command{
    Use:   "completion [bash|zsh|fish|powershell]",
    Short: "Generate shell completion script",
    Long:  `Output shell completion code for the specified shell.`,
}

func init() {
    rootCmd.AddCommand(completionCmd)
    completionCmd.AddCommand(&cobra.Command{
        Use:   "bash",
        Short: "Generate bash completion",
        RunE: func(cmd *cobra.Command, args []string) error {
            return rootCmd.GenBashCompletion(os.Stdout)
        },
    })
    // zsh, fish, powershell similarly
}
```

**测试**：`go build ./...` + 运行 `newapi-tools completion bash` 确认输出

### Task V2: Docker API 集成评估

**文件**：`internal/docker/http_client.go` + `internal/docker/sdk_client.go`  
**目标**：评估 TRAE 编写的 HTTP 客户端是否可以替代现有 exec.Command

1. 检查 http_client.go 是否覆盖全部现有操作（ContainerList, ImagePull, ComposeUp）
2. 编写集成测试，在 Linux 实机验证 HTTP 端点连通性
3. 输出评估报告：是否可以替换 / 风险点 / 性能对比

### Task V3: 新增 config validate 子命令

**文件**：`internal/cli/config.go`  
**工作量**：⭐

基于 `core.NewAPIConfig.Validate()` 已有实现，新增 CLI 入口：

```go
var configValidateCmd = &cobra.Command{
    Use:   "validate",
    Short: "Validate configuration",
    RunE:  runConfigValidate,
}

func runConfigValidate(cmd *cobra.Command, args []string) error {
    cfg := core.GetConfig()
    if cfg == nil {
        return fmt.Errorf("configuration not loaded")
    }
    if err := cfg.NewAPI.Validate(); err != nil {
        fmt.Printf("❌ Configuration invalid: %v\n", err)
        return err
    }
    fmt.Println("✅ Configuration is valid.")
    return nil
}
```

### Task V4: 补齐 osutil 测试至 ≥30%

**文件**：`internal/osutil/osutil_test.go`（当前 28.4%）
**目标**：覆盖平台检测函数（Detect、IsDebian、IsRHEL 等）

### Task V5: 补齐 cli 测试至 ≥30%

**文件**：`internal/cli/cli_more_test.go`（当前 17.4%）
**目标**：增加 config validate、instance、audit list 等命令注册测试
